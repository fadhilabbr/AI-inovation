[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -c \"import os; import subprocess; port = os.environ.get('PORT', '8000'); subprocess.run(['uvicorn', 'main:app', '--host', '0.0.0.0', '--port', port])\""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
