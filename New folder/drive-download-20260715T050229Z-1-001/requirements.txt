# requirements.txt — Dependensi program deteksi sampah real-time
#
# Install semua dependensi:
#   pip install -r requirements.txt
#
# Untuk lingkungan virtual (disarankan):
#   python -m venv venv
#   source venv/bin/activate   # Linux/macOS
#   venv\Scripts\activate      # Windows
#   pip install -r requirements.txt

# OpenCV — capture kamera dan rendering overlay
opencv-python>=4.9.0

# Google Generative AI SDK — integrasi Gemini API
google-generativeai>=0.8.0

# Pillow — konversi frame ke format PIL Image untuk Gemini API
Pillow>=10.0.0
